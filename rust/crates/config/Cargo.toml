[package]
name = "tt-config"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "typed Config parsed from environment"

[dependencies]
serde = { version = "1", features = ["derive"] }
