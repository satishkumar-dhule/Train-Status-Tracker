[package]
name = "tt-provider-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "TrainStatusProvider trait, KnownTrain, provider error enum"

[dependencies]
async-trait = "0.1.91"
thiserror = "2.0.19"
tokio = { version = "1.53.1", features = ["sync"] }
tt-mapper = { version = "0.1.0", path = "../mapper" }

[dev-dependencies]
tokio = { version = "1.53.1", features = ["sync", "macros", "rt-multi-thread", "time"] }
