[package]
name = "tt-mapper"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "MappedStatus/MappedStation normalization contract (ZTA untrusted payloads)"

[dependencies]
serde_json = "1.0.151"
tt-contract = { version = "0.1.0", path = "../contract" }
