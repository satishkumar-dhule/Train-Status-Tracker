[package]
name = "tt-contract"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "serde wire types + query-param validation (zod-coercion semantics)"

[dependencies]
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
