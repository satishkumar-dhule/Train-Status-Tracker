#!/usr/bin/env bash
# Parity harness (Slice 13): boots the TS reference API server and the Rust
# API server, then diffs every endpoint's responses byte-for-byte.
# Stubbed in Slice 1. Implemented as part of the E2E parity slice.
set -euo pipefail

echo "parity: not yet implemented (Slice 13)"
exit 0
